// Modules
// CmmonJS,  every file is a module (by default)
// Modules - Encapsulated Code (only share minimum)

const names = require('./names')
const sayHi = require('./utils')
const data = require('./alternative-flavour')
// console.log(names)
// sayHi(names.john)
// sayHi(names.peter)
// console.log(data)
require('./mind-grenade')